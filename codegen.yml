overwrite: true
schema: ./server/graphql/schema.graphql
documents: ./server/graphql/queries/*.graphql
generates:
  ./client/gen/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
  ./server/gen/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
