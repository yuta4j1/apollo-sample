query FetchBooks($title: String) {
  books(title: $title) {
    title @client
    author {
        id
        name
        detail {
            birthday
            sex
        }
    }
  }
}

query FetchPublishers {
  publishers {
    id
    name
    address
    capital
  }
}
